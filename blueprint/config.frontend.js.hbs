
var serverConfig = require('./server');

module.exports = {
    name: serverConfig.name,
    apiURI: '/api/'+serverConfig.version,

    test: {
        apiURL: 'http://localhost:'+serverConfig.port+'/api/'+serverConfig.version
    }
};