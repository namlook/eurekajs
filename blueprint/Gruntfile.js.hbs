module.exports = function(grunt) {

    // loading eureka tasks
    require('eurekapi/grunt-tasks')(grunt, require('./config/vendors-list'));

    grunt.extendConfig({
        /** insert your configs here **/
    });

    /** insert your tasks below **/
    grunt.registerTask('build-development', ['eureka:build']);
    grunt.registerTask('build-test', ['eureka:build']);
    grunt.registerTask('build-production', ['eureka:setenv-production', 'eureka:clean', 'eureka:configure', 'eureka:dist']);
    grunt.registerTask('live', ['eureka:live']); // TODO ['build', 'eureka:live']
    grunt.registerTask('test', ['eureka:test']);
    grunt.registerTask('build', function(target) {
        var environment = process.env.NODE_ENV || 'development';
        environment = environment.toLowerCase();
        grunt.task.run('build-'+environment);
    });
};